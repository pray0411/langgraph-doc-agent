# 复制为 .env 后按需修改。所有键都有代码内默认值，因此本文件不是"必填清单"，
# 而是"可用开关清单"——此前这里漏掉了一半键（外部评审指出），
# 导致用户以为某些配置项不存在（如 UPLOADS_DIR / GLOBAL_MEMORY_DB）。

# ===== 1) 模型配置（多选一，用 LLM_PROVIDER 指定）=====
# deepseek / openai / qwen / zhipu / moonshot / ollama
LLM_PROVIDER=deepseek
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx
LLM_MODEL=deepseek-chat
# 可选：自定义 OpenAI 兼容网关地址。配了它就会覆盖上面服务商的默认地址
# （修复过的问题：此前该变量被服务商预设的 default_base_url 永久遮蔽，等于死代码）
# LLM_BASE_URL=https://your-gateway.example.com/v1

# 其他服务商的 Key（按需开启；键名必须与 config.PROVIDER_PRESETS 里的 api_key_env 一致）
# OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxx
# QWEN_API_KEY=sk-xxxxxxxxxxxxxxxx
# ZHIPU_API_KEY=xxxxxxxxxxxxxxxx.xxxxxxxx
# MOONSHOT_API_KEY=sk-xxxxxxxxxxxxxxxx

# 本地 Ollama（免费离线，需自行安装 Ollama 并已拉取模型）
# OLLAMA_BASE_URL=http://localhost:11434/v1
# OLLAMA_MODEL=qwen2.5:7b

# ===== 2) 检索配置 =====
# 混合检索：BM25（jieba 分词）+ 语义向量（sentence-transformers，本地运行）
# 语义模型不可用时**自动回退纯 BM25**（此时"混合检索"实际只有 BM25 在跑）
TOP_K=3
# 分块
CHUNK_SIZE=500
CHUNK_OVERLAP=100
# 检索最低融合分数阈值。RRF 融合分恒为正，故 0.0 = 关闭阈值（不过滤任何结果）
MIN_SCORE=0.0
# 语义模型名（首次运行会下载到 models/，约数百 MB）
# EMBEDDING_MODEL=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
# 显式关闭语义通道 → 只跑纯 BM25。
# 用途：让指标可跨机器比较。装了 sentence-transformers 就走混合检索、没装就走纯 BM25，
# 两条路径的 recall/MRR 不同 —— 评估与 CI 用这个开关把基准钉死在一处。
# 当前实际生效的模式可用 retriever.retrieval_mode() 查看（"hybrid" / "bm25"）。
# DISABLE_SEMANTIC=1

# ===== 3) 存储位置 =====
# Agent 可写目录（write_file / run_command / 终端的工作目录，也是路径安全边界）
# WRITE_DIR=./generated
# 上传文档目录（默认在 WRITE_DIR 内；http 落盘与检索重建索引必须指向同一处）
# UPLOADS_DIR=./generated/uploads
# 会话记忆（LangGraph checkpointer，按 thread_id 持久化，重启不丢）
# MEMORY_DB=./data/memory.sqlite
# 全局记忆（跨会话的用户画像 / 长期事实，与会话记忆分库存储）
# GLOBAL_MEMORY_DB=./data/global_memory.sqlite

# ===== 4) 安全 =====
# API Token。**未设置时默认不鉴权**，此时安全完全依赖两件事：
#   1) 服务只绑回环地址（默认 127.0.0.1）
#   2) 来源校验（Origin / Host / Sec-Fetch-Site）挡住跨站请求
# 只要把服务暴露到局域网或公网，就**必须**设置它。
# API_TOKEN=your-secret-token

# 显式的来源白名单（逗号分隔的 host 或 host:port）。
# 仅在"局域网 IP 访问 / 自定义域名 / 反向代理"这类场景需要；
# 回环地址与本机网卡地址已自动放行，不需要写在这里。
# ALLOWED_ORIGINS=192.168.1.5:8000,app.internal

# ===== 5) 联网搜索 =====
# 博查 Bocha API Key（中文搜索质量高，Agent 专用）：https://open.bochaai.com
# 配置后 web_search 优先用博查；未配置则退回 DuckDuckGo（ddgs）
# BOCHA_API_KEY=sk-xxxxxxxxxxxxxxxx
